{
  "name": "@nervue/root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev",
    "test": "lerna run test --scope=nervue",
    "setup": "lerna exec npm install && lerna bootstrap --hoist",
    "bootstrap": "lerna bootstrap",
    "docs:dev": "lerna run docs:dev",
    "docs:build": "lerna run docs:build",
    "docs:serve": "lerna run docs:serve",
    "lint:editorconfig": "editorconfig-checker"
  },
  "devDependencies": {
    "editorconfig-checker": "^4.0.2",
    "eslint": "^7.32.0",
    "eslint-loader": "^4.0.2",
    "eslint-plugin-vue": "^8.4.1",
    "lerna": "^5.1.4",
    "regenerator-runtime": "^0.13.9",
    "typescript": "^4.5.5"
  }
}
